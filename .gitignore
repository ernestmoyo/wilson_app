# Personal notes
notes.txt

# Private client case files — never commit
/client-cases/
abecca/
Zhendi Yang/
Location 2_ Cool 2/

# Correspondence and reference docs (local only)
/correspondance/
/reference/

# Assure Safety NZ business documents (local only)
/assure-safety-nz/

# Document file types (sensitive)
*.pdf
*.docx
*.doc
*.xlsx
*.xls
*.xlsm
*.msg
*.HEIC
*.jpeg
*.jpg
*.zip
*.png

# Dependencies
node_modules/
**/node_modules/

# Build outputs
dist/
build/
**/dist/
**/build/

# Database files (contain real client data)
*.sqlite
*.sqlite3
*.db

# Environment variables
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/settings.json
.idea/

# TypeScript cache
*.tsbuildinfo

# Generated documents
Wilson_Suite_SOP.docx
generate-sop.mjs
*.sqlite-shm
*.sqlite-wal

# ── platform (feat/graph-model) ──────────────────────────────────────────
# Flutter web build copied here for Vercel; must be committed whole, icons included.
!apps/server/public/
!apps/server/public/**
# Build-time vendored assets and local evidence store — never committed.
apps/server/vendor/
apps/server/data/
# Extracted workbook evidence (real client site photos) and signature stay local.
packages/checksheets/data/evidence/
packages/checksheets/data/certificates/*/signature.png

# Vercel CLI link metadata
.vercel/
# Rendered certificate output embeds the signature as a data URL — keep out of git.
packages/checksheets/data/certificates/*/certificate.html
